#include "main.h"
#include <stdio.h>

/**
 * add - function adds two numbers together
 *
 * @a: accepts integer as input
 * @b: accepts integer as input
 * Return: Always 0
 */

int add(int a, int b)
{

	return (a + b);
}
